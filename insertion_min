def insertion(lista):
    n = len(lista)
    for i in range(1, n):
        for j in range(n):
            if lista[i] < lista[j]:
                lista[i], lista[j] = lista[j], lista[i]
    return lista
